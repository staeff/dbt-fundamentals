version: 2

sources:
    - name: stripe
      description: Payment data from original Postgres DB.
      database: acoustic-realm-340815
      schema: dbt
      tables:
        - name: payment
          loaded_at_field: _batched_at
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}